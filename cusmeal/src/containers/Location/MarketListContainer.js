import MarketListComponent from '../../components/Location/MarketListComponent';

const MarketListContainer = ({history}) => {
    return(
        <>
            <MarketListComponent history={history}/>
        </>
    )
}

export default MarketListContainer;